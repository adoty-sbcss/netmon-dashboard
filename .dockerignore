node_modules
.next
.git
.gitignore
npm-debug.log*
README.md
AGENTS.md
CLAUDE.md
docs
# NOTE: drizzle/ must stay in the build context — the migrator image runs
# `drizzle-kit migrate`, which reads the SQL files in drizzle/. The runtime
# (runner) stage doesn't copy it, so this costs the lean image nothing.
.env
.env.*
!.env.example
*.tsbuildinfo
.vscode
.idea
.DS_Store
Thumbs.db
coverage
